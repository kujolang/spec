# Examples

These are canonical, copyable specs for users and agents.

- `basic.spec.yml` is the shortest full-feature example to imitate first.
- `complex.spec.yml` shows a larger multi-system task and is intentionally more complete.

Fixtures in `../fixtures/` exist for parser and validation coverage. Generated output, release checklists, and expected-fail data should not be copied as style examples.
